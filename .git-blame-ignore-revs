# Commits that touched nearly every line without changing what the code does.
# git blame skips them once you run, once per clone:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub's blame view reads this file on its own.  Add a line here whenever a
# mechanical sweep lands (a reformat, a header rewrite); never list a commit
# that changed behavior.

# chore: drop the per-file author lists from every source header (#437)
458599902c3c7a1c7a3be7338d8c8bf51cfeb99c

# chore: normalize whitespace in every source file (tabs -> 4 spaces, no trailing whitespace)
5aa88f2ce6795aacbf04896fdc7a9a7391d55f1d
